import theme from 'util/theme'

const styles = {
  headerProfileRoot: {
    display: 'flex',
    alignItems: 'center'
  },
  headerIcon: {
    margin: '0.25rem',
    color: theme.colors.charcoal
  },
  headerAccount: {
    margin: '0.25rem',
    color: theme.colors.hazygrey
  },
  headerText: {
    margin: '0.25rem'
  }
}

export default styles
