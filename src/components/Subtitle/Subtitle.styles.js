import uiTheme from 'util/theme'

const styles = () => ({
  title: { display: 'flex', alignItems: 'center', padding: '1rem 0' },
  titleText: {
    fontSize: '20px',
    color: uiTheme.colors.charcoal,
    fontWeight: 500
  }
})

export default styles
